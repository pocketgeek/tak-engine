# Original game assets — never commit Cavedog's copyrighted content
assets/

# Build output
build/
.cache/
compile_commands.json

# User audio override archives (game-derived, keep local)
*.hpi
*.HPI
